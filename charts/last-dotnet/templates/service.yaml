apiVersion: v1
kind: Service
metadata:
  name: {{ include "last-dotnet.fullname" . }}
  labels:
    {{- include "last-dotnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "last-dotnet.selectorLabels" . | nindent 4 }}
